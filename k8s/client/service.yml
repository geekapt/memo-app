apiVersion: v1
kind: Service
metadata:
  name: memo-app-frontend
  namespace: memo-app
spec:
  selector:
    app: memo-app-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  # type: ClusterIP
