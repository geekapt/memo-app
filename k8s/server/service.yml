apiVersion: v1
kind: Service
metadata:
  name: memo-app-backend
  namespace: memo-app
spec:
  selector:
    app: memo-app-backend
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
  # type: ClusterIP
